# flask-property-management

## API REST to manage properties having these features :
- Handle Users
- Handle properties
- Filter by cities
- Modify properties only by its owner


## Install and run
```bash
python -m venv venv
source venv/bin/activate  # or venv\Scripts\activate sur Windows
pip install -r
flask --app api run --debug
```

